(function carousel() {
    'use strict;'

    angular.module('nmlApp')
        .factory('NmlCarousel', carouselService);

    carouselService.$inject = [];

    function carouselService() {
        return {
        };
    }
})();
